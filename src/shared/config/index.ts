// Re-exports from shared constants for FSD layer compatibility.
// All constants live here; features import from '@shared/config'.
export { STORAGE_KEYS, JOIN_REASONS, VIBES, MOCK_INTERESTS } from '../../constants';
