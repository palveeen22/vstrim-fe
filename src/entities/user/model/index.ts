// User entity — all User-related types come from shared/types
// to avoid duplication. Import from here in features/widgets.
export type {
  User,
  UserCoordinates,
  UserHangoutPlace,
  UserCommunity,
  UserEvent,
  UserMatch,
  Match,
  DailyQuiz,
  QuizQuestion,
  QuizAnswer,
  QuizOption,
  MatchStatus,
  HangoutPlaceType,
  JoinReason,
  VibeType,
  InterestCategory,
} from '../../../shared/types';
