// Match entity — types re-exported from the match feature's model layer.
export type {
  MatchResponse,
  MatchData,
  MatchItem,
  Entity,
  EntityBase,
  EntityType,
  UserEntity,
  EventEntity,
  CommunityEntity,
  PlaceEntity,
  GenericEntity,
  GroupedResults,
  Stats,
  Breakdown,
} from '../../../features/match/model/content';
